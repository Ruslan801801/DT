# Экран “Рядом” — стартовая страница DeepTea

Этот документ фиксирует UI/UX-спецификацию стартового экрана “Рядом” (Nearby),
который объединяет BLE-сканирование, быстрые чаевые, офлайн-ваучеры и сигналы риск-движка.

## Цели экрана

1. Дать “момент магии”: увидеть людей/профили рядом и отправить чаевые за 2–3 тапа.
2. Сохранить честность BLE: показывать **качество близости**, а не “точные метры”.
3. Ясно объяснить офлайн-режим и статус ваучеров.
4. Избежать утечек приватности: данные по умолчанию минимальны.

## Ключевые состояния

### S1 — Активное сканирование, устройств нет
- Hero-анимация “радар”.
- Текст:
  - “Ищем устройства рядом…”
  - “Поднесите телефон ближе”.
- CTA: “Как это работает?” (ссылка на Help).

### S2 — Активное сканирование, есть устройства
- Hero блок уменьшается (compact).
- Список карточек сортируется по “confidence”:
  - свежесть появления
  - качество RSSI
  - частота “пингов”.

### S3 — Офлайн-режим
- Баннер:
  - “Нет интернета. BLE работает. Можно отправлять офлайн-ваучеры.”
- Виджет:
  - количество pending ваучеров
  - кнопка “Синхронизировать при сети”.

### S4 — Предупреждение риска
- Если risk-engine отдаёт `risk_level >= medium`:
  - карточка остаётся видимой
  - CTA меняется на “Отправить с подтверждением”.
  - краткое объяснение причины (например: “сигнал нестабилен”).

### S5 — Пустой список + выключенный BLE
- Диалог разрешений:
  - Bluetooth
  - Location (если требуется платформой)
  - Background mode (позже, в продвинутом онбординге).

## Компоненты

- `HeaderBrand`
  - логотип 🍵, подпись “P2P чаевые по Bluetooth”
  - micro-copy: “Офлайн • Быстро • Надёжно”

- `ScanHero`
  - круговая анимация/пульс
  - счётчик: “Найдено: N”
  - кнопка паузы сканирования

- `DeviceList`
  - виртуализированный список

- `DeviceCard`
  - аватар/иконка роли
  - displayName
  - tagline (1 строка)
  - `SignalPill`
  - `LiveBadge`
  - `QuickAmountsRow`
  - primary CTA

- `QuickSendDock`
  - фиксированный нижний блок с пресетами
  - активируется после выбора получателя

- `PendingVouchersMini`
  - показывается только если `pending_count > 0`

## Карточка устройства — контент и правила

**Поля (MVP):**
- `displayName`
- `role/category` (опционально)
- `tagline` (опционально)
- `signal_quality`
- `last_seen`

**Запреты по умолчанию:**
- точный адрес
- точные метры
- публичная история чаевых

**Допускается при явном согласии пользователя:**
- “место работы”
- “любимая точка”
- короткий публичный статус.

## Визуальные правила сигнала

Показывать качество, и только вторым слоем — dBm:

- Отличный
- Хороший
- Средний
- Слабый

Пример:
- `📶 Хороший (-55 dBm)`

## Тайминги и “живость”

- Обновление списка: 0.5–1.0 сек.
- TTL карточки без обновлений: 8–12 сек.
- Метка:
  - **NOW LIVE**: видели < 3 сек назад
  - “видели недавно”: 3–10 сек
  - дальше — скрывать.

## Маппинг на backend

- Если устройство содержит EID:
  - `POST /api/ble/resolve`
- Перед отправкой чаевых:
  - `POST /api/risk/decide`
- Онлайн чаевые:
  - `POST /api/p2p/create`
  - с `Idempotency-Key`
- Офлайн:
  - `POST /api/voucher/issue` (или локальная генерация + later redeem)

## Микрокопирайтинг

- Сканирование:
  - “Ищем людей рядом…”
- Офлайн:
  - “Нет интернета — отправим ваучер и синхронизируем позже.”
- Риск:
  - “Сигнал нестабилен. Проверьте, что вы рядом.”

## ASCII-референс

Ниже сохраняется текстовый мокап как дизайн-референс для обсуждений:
