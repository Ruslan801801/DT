# DT Mobile Demo Screen

1) Run backend with DEMO_MODE=1
2) Open the app -> set Base URL:
- http://localhost:3000
- or http://localhost:3000/api (if server uses /api prefix)
3) Tap: Health -> Login -> Send -> Feed
